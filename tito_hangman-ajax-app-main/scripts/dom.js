let DOM = {
    OcultarPelicula(Pelicula,peliculaAsterisco, e){
        
     /*   
    let letra = e.key;

        for(var i = 0; i < Pelicula.length; i++){
            if(Pelicula[i] == letra){
                STATE.movieGuess[i] = letra;
            }
        }
        STATE.checkedLetters.push(letra);
        
        $("#letters-tried").text(STATE.checkedLetters.join(" "));
        console.log(STATE.movieGuess);
        //$("#puzzle").text(Pelicula.join(" "));
       */
    }
    
}